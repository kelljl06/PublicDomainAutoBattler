using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MMLoad : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //GameObject screenPet = Resources.Load(new Winnie().prefab) as GameObject;
        //GameObject onscreen = Instantiate(screenPet, new Vector3(0,0,0), transform.localRotation);

        //onscreen.transform.localScale = new Vector3(10, 10, 0);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
